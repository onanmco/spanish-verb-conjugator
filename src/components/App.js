import React, { useState } from "react";
import Conjugator from "./Conjugator";
import '../assets/css/style.css';

export default () => {
    return (
        <div id="container">
            <Conjugator />
        </div>
    );
};